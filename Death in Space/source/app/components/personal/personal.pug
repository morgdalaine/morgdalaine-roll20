
mixin origin_benefit_block
  div.stat-block.origin-benefit-block
    +stat_area('origin_benefit', 'origin-benefit', 10)

mixin xp_block
  div.stat-block.xp-block
    +stat('number', 'xp', 'stat-xp')

mixin background_block
  .stat-block.background-block
    +stat('text', 'background', 'stat-background')

mixin past_allegiance_block
  .stat-block.past-allegiance-block
    +stat('text', 'past', 'stat-past-allegiance')

mixin trait_block
  .stat-block.trait-block
    +stat('text', 'trait', 'stat-trait')

mixin drive_block
  .stat-block.drive-block
    +stat('text', 'drive', 'stat-drive')

mixin looks_block
  .stat-block.looks-block
    +stat_area('looks', 'stat-looks', 4)
